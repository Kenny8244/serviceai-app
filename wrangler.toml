# Cloudflare Pages - Vite SPA + Functions proxy to the API Worker
name = "serviceai-app"
compatibility_date = "2026-08-25"
pages_build_output_dir = "dist"

# Same-account call: Pages /api/* - serviceai-api
[[services]]
binding = "API"
service = "serviceai-api"
